ip tunnel add {{ .TunnelName }} mode sit remote {{ .IPv4Remote }} local {{ .IPv4Local }}
ip link set {{ .TunnelName }} up
ip addr add {{ .P2P }} dev {{ .TunnelName }}
ip -6 route add {{ .PD }} dev {{ .TunnelName }}
